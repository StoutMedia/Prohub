import Card from '../../components/ui/Card.jsx';
import { sessionBlocks } from '../../data/mockTraining.js';
export default function SessionPlanner(){return <div className="page-stack"><div className="dashboard-title"><p className="eyebrow">Session Planner</p><h1>Coach-facing quick build</h1></div><Card><label>Session title<input defaultValue="MD-3: Build + Break Lines"/></label><label>Game moment<input defaultValue="Possession with Purpose to Attack"/></label><label>Tactical principle<input defaultValue="Create and exploit space"/></label><label>Technical focus<input defaultValue="Scanning, body orientation, preparation touch"/></label>{sessionBlocks.map(block=><div className="list-row" key={block.block}><div><strong>{block.block}</strong><p>{block.detail}</p></div><span>{block.duration} min</span></div>)}<label>Coaching points<textarea defaultValue="Scan before receiving; open body shape; play through pressure; counterpress on loss."/></label><label>KPIs<textarea defaultValue="8+ line breaks into zone 14; 5+ regains within 5 seconds; 70% forward receiving actions."/></label><button className="btn btn-primary">Save/export placeholder</button></Card></div>}
