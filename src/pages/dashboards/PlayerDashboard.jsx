import Card from '../../components/ui/Card.jsx';
import StatCard from '../../components/ui/StatCard.jsx';
import Badge from '../../components/ui/Badge.jsx';
import ProgressBar from '../../components/ui/ProgressBar.jsx';
import { players, trainingPlans, recruitingContacts, reports } from '../../data/pisaData.js';
export default function PlayerDashboard(){const player=players[0];return <div className="page-stack"><div className="dashboard-title"><p className="eyebrow">Player Dashboard</p><h1>Welcome back, {player.name}.</h1><p>Your profile, IDP, training, recruiting, and reports are connected in one command center.</p></div><div className="card-grid four-grid"><StatCard label="IDP progress" value={`${player.idpProgress}%`}/><StatCard label="Recruiting progress" value={`${player.recruitingProgress}%`} tone="blue"/><StatCard label="Attendance" value={`${player.attendance}%`} tone="green"/><StatCard label="Wellness" value={player.wellness} tone="purple"/></div><div className="dashboard-grid"><Card><div className="snapshot-head"><span className="avatar large">{player.avatar}</span><div><h2>{player.position}</h2><p>{player.team} • Class of {player.graduationYear} • GPA {player.gpa}</p></div></div><ProgressBar label="Development pathway" value={player.idpProgress}/><ProgressBar label="College recruiting CRM" value={player.recruitingProgress}/></Card><Card><h2>This week</h2><Badge>{trainingPlans[0].completionStatus}</Badge><h3>{trainingPlans[0].theme}</h3><p>{trainingPlans[0].objective}</p></Card><Card><h2>Recruiting next action</h2><p><strong>{recruitingContacts[0].collegeName}</strong></p><p>{recruitingContacts[0].nextAction}</p><Badge tone="orange">{recruitingContacts[0].interestLevel} interest</Badge></Card><Card><h2>Latest report</h2><p>{reports[0].summary}</p><Badge tone="green">Coach recommendation ready</Badge></Card></div></div>}
