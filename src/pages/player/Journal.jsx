import Card from '../../components/ui/Card.jsx';
import { journalEntries } from '../../data/mockTraining.js';
export default function Journal(){return <div className="page-stack"><div className="dashboard-title"><p className="eyebrow">Training Journal</p><h1>Reflect, learn, repeat.</h1></div><div className="dashboard-grid">{journalEntries.map(entry=><Card key={entry.date}><h2>{entry.date}</h2><p><strong>Reflection:</strong> {entry.reflection}</p><p><strong>What went well:</strong> {entry.wentWell}</p><p><strong>Needs improvement:</strong> {entry.improve}</p><p>Confidence {entry.confidence}/10 • Effort {entry.effort}/10</p><p><strong>Coach feedback:</strong> {entry.coachFeedback}</p></Card>)}<Card><h2>New journal prompt</h2><label>Session reflection<textarea placeholder="What happened in today’s session?" /></label><label>Confidence rating<input type="range" min="1" max="10" /></label></Card></div></div>}
