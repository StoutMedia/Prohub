import Card from '../../components/ui/Card.jsx';
import ProgressBar from '../../components/ui/ProgressBar.jsx';
import { players, developmentGoals } from '../../data/mockPlayers.js';
export default function DevelopmentPlan(){const player=players[0];return <div className="page-stack"><div className="dashboard-title"><p className="eyebrow">Player Development Plan</p><h1>Individual Development Plan</h1><p>Strengths, areas for improvement, monthly objectives, and coach notes.</p></div><div className="dashboard-grid"><Card><h2>Strengths</h2><ul>{player.strengths.map(x=><li key={x}>{x}</li>)}</ul></Card><Card><h2>Areas for improvement</h2><ul>{player.improvements.map(x=><li key={x}>{x}</li>)}</ul></Card><Card><h2>Goals</h2>{developmentGoals.map(goal=><ProgressBar key={goal.area} label={`${goal.area}: ${goal.goal}`} value={goal.progress}/>)}</Card><Card><h2>Coach notes</h2><p>Monthly objective: receive side-on, scan early, and break the first line with purpose. Connect technical detail to tactical advantage.</p></Card></div></div>}
