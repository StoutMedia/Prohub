import { OnboardingProgress } from '@/components/onboarding/Progress';
import { ChoiceStep } from '@/components/onboarding/StepForm';
export default function WorkspacePage() { return <><OnboardingProgress current={2} /><ChoiceStep field="workspace_type" next="/onboarding/profile" title="Select workspace" subtitle="Invited users skip public pricing after invite acceptance." options={[{ label:'Individual', value:'individual', description:'Player, parent, or independent coach workspace.' },{ label:'Organization', value:'organization', description:'Set up a club, academy, or team workspace.' },{ label:'I received an invite', value:'invite', description:'Join an existing organization with your invite token.' },{ label:'PISA Player', value:'pisa', description:'PISA player onboarding path.' }]} /></>; }
