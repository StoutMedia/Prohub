'use client';
import { useState } from 'react';
import { Button } from '@/components/ui/Button';
import { OnboardingProgress } from '@/components/onboarding/Progress';
export default function InviteUsersPage() { const [msg,setMsg]=useState(''); async function submit(formData: FormData){ const res=await fetch('/api/invites/create',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(formData.entries()))}); const data=await res.json(); setMsg(res.ok?`Invite created: ${data.inviteUrl}`:data.error); } return <><OnboardingProgress current={4}/><h1 className="text-3xl font-black text-[#0B2751]">Invite users</h1>{msg&&<p className="mt-4 rounded-xl bg-slate-100 p-3 text-sm break-all">{msg}</p>}<form action={submit} className="mt-6 grid gap-4"><input name="email" type="email" required placeholder="Invite email" className="focus-ring rounded-xl border border-slate-200 px-4 py-3"/><select name="role" className="focus-ring rounded-xl border border-slate-200 px-4 py-3"><option value="coach">Coach</option><option value="player">Player</option><option value="parent">Parent</option><option value="staff">Staff</option></select><Button>Create invite</Button></form></>; }
